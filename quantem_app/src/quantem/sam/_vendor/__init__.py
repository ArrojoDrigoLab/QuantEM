"""Third-party source copied into QuantEM rather than installed as a package.

One entry: :mod:`quantem.sam._vendor.segment_anything`. Vendored files are kept
byte-identical to upstream wherever importing allows, so they can be diffed
against the source; the few unavoidable changes carry a ``QUANTEM:`` comment.
"""
